export const rectArea = (b: number, h: number) => b * h
export const parallelogramArea = (b: number, h: number) => b * h
export const triangleArea = (b: number, h: number) => (b * h) / 2
export const trapeziumArea = (a: number, b: number, h: number) => ((a + b) / 2) * h
